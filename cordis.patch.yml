# dsh-plugin-text-quote bundle patch: self-insert so the loader enables the
# plugin row when this package is added to a profile (dsh plugin add).
- insert:
    - id: dsh-plugin-text-quote
      name: 'dsh-plugin-text-quote'
